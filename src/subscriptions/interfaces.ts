export enum SubscriptionStatus {
  ACTIVE = 'active',
  EXPIRED = 'expired',
  PENDING = 'pending',
  CANCELED = 'canceled',
  TRIAL = 'trial',
}

export enum SubscriptionPeriod {
  MONTHS = 'months',
  YEARS = 'years',
}
